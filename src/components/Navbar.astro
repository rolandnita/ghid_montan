---
import { SITE_CONFIG } from '../utils/config';

interface Props {
  currentPath?: string;
}

const { currentPath = '' } = Astro.props;

// Ensure BASE_URL always ends with "/"
const baseUrl = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;

const buildUrl = (path: string = '') => `${baseUrl}${path}`;

const navItems = [
  { label: 'Home', href: buildUrl() },
  { label: 'Despre', href: buildUrl('about/') },
  { label: 'Tururi', href: buildUrl('tours/') },
  { label: 'Academia', href: buildUrl('academy/') },
  { label: 'Contact', href: buildUrl('contact/') },
];

const normalizePath = (path: string) =>
  path.replace(/\/+$/, '');

const isActive = (href: string) => {
  const current = normalizePath(currentPath);
  const target = normalizePath(href);

  if (target === normalizePath(baseUrl)) {
    return (
      current === '' ||
      current === normalizePath(baseUrl)
    );
  }

  return current.startsWith(target);
};
---

<nav class="sticky top-0 z-40 w-full backdrop-blur-xl bg-slate-950/80 border-b border-slate-800/50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-20">
      <!-- Logo -->
      <a href={baseUrl} class="flex items-center gap-2 group">
        <div class="w-10 h-10 bg-gradient-to-br from-amber-600 to-amber-500 rounded-lg flex items-center justify-center">
          <span class="text-white font-bold text-lg">⛏</span>
        </div>
        <span class="text-xl font-bold tracking-tight text-slate-100 hidden sm:inline group-hover:text-amber-400 transition-colors">
          {SITE_CONFIG.siteName}
        </span>
      </a>

      <!-- Desktop menu -->
      <div class="hidden md:flex items-center gap-1">
        {navItems.map((item) => (
          <a
            href={item.href}
            class={`px-4 py-2 rounded-lg transition-all duration-300 ${
              isActive(item.href)
                ? 'text-amber-400 bg-amber-500/10 border border-amber-500/30'
                : 'text-slate-300 hover:text-slate-100 hover:bg-slate-800/50'
            }`}
          >
            {item.label}
          </a>
        ))}
      </div>

      <!-- Mobile menu button -->
      <div class="md:hidden">
        <button
          id="mobile-menu-btn"
          class="p-2 rounded-lg text-slate-300 hover:text-slate-100 hover:bg-slate-800/50 transition-colors"
          aria-label="Toggle menu"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile menu -->
    <div id="mobile-menu" class="hidden md:hidden pb-4 border-t border-slate-800/50">
      {navItems.map((item) => (
        <a
          href={item.href}
          class={`block px-4 py-2 rounded-lg transition-all duration-300 ${
            isActive(item.href)
              ? 'text-amber-400 bg-amber-500/10 border border-amber-500/30 m-2'
              : 'text-slate-300 hover:text-slate-100 hover:bg-slate-800/50 m-2'
          }`}
        >
          {item.label}
        </a>
      ))}
    </div>
  </div>
</nav>

<script>
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');

  if (mobileMenuBtn && mobileMenu) {
    mobileMenuBtn.addEventListener('click', () => {
      mobileMenu.classList.toggle('hidden');
    });

    // Close menu when link is clicked
    mobileMenu.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => {
        mobileMenu.classList.add('hidden');
      });
    });
  }
</script>
